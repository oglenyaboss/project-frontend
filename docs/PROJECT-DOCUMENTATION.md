# 📋 Техническая документация проекта

## 🎯 Назначение

**CJM Platform** — веб-приложение для автоматизированного анализа бизнес-требований с использованием AI-агента. Платформа позволяет проводить интерактивные интервью, собирать требования и генерировать структурированную документацию.

---

## 🏗️ Архитектурные решения

### Feature-Sliced Design (FSD)

Проект построен на методологии **Feature-Sliced Design** — современном архитектурном подходе для масштабируемых фронтенд-приложений.

```
src/
├── app/          # Страницы и роутинг (Next.js App Router)
├── widgets/      # Композитные UI-блоки (AppSidebar, Chat, SettingsPanel)
├── features/     # Пользовательские действия (auth, chat-message, create-project)
├── entities/     # Бизнес-сущности (user, project, agent-session)
└── shared/       # Переиспользуемый код (UI, hooks, lib, store, api, config)
```

**Преимущества FSD:**
- ✅ **Предсказуемость** — всегда понятно, где искать код
- ✅ **Изоляция** — изменения в одном модуле не влияют на другие
- ✅ **Масштабируемость** — легко добавлять новые фичи
- ✅ **Тестируемость** — каждый модуль можно тестировать отдельно
- ✅ **Онбординг** — новые разработчики быстро понимают структуру

### Backend For Frontend (BFF)

Реализован паттерн **BFF** через Next.js API Routes:

```
src/app/api/
├── auth/           # Авторизация (login, logout, register, refresh, me)
├── agent/          # AI-агент (sessions, messages, context)
├── projects/       # Управление проектами
├── requirements/   # Генерация требований
└── user/           # Профиль пользователя
```

**Преимущества BFF:**
- 🔐 **Безопасность** — токены в httpOnly cookies, защита от XSS
- 🚀 **Производительность** — возможность кэширования и агрегации
- 🌐 **CORS** — все запросы с того же домена
- 🔧 **Гибкость** — трансформация данных под нужды UI

### State Management — Zustand

Легковесное управление состоянием без boilerplate:

```typescript
// Пример: Auth Store с персистентностью
export const useAuthStore = create<AuthState>()(
  persist(
    (set) => ({
      user: null,
      isAuthenticated: false,
      login: async (credentials) => { /* ... */ },
      logout: async () => { /* ... */ },
    }),
    { name: "auth-storage" }
  )
);
```

**Преимущества Zustand:**
- 📦 Минимальный boilerplate
- 🔒 TypeScript из коробки
- ⚡ Оптимизация ре-рендеров через селекторы
- 💾 Middleware для персистентности

---

## 🛠️ Технологический стек

| Категория | Технология | Версия | Назначение |
|-----------|------------|--------|------------|
| **Фреймворк** | Next.js | 15.5.9 | React-фреймворк с App Router |
| **Язык** | TypeScript | 5.x | Статическая типизация |
| **Стилизация** | Tailwind CSS | 4.x | Utility-first CSS |
| **UI-компоненты** | Radix UI + shadcn/ui | - | Headless + готовые паттерны |
| **State** | Zustand | 5.0.8 | Глобальное состояние |
| **Data Fetching** | TanStack Query | 5.90.11 | Серверное состояние, кэширование |
| **Формы** | react-hook-form + zod | 7.65 / 3.25 | Валидация и управление формами |
| **Анимации** | Framer Motion | 12.23 | Декларативные анимации |
| **3D** | React Three Fiber + Drei | 9.4 / 10.7 | 3D-графика (логотип) |
| **Тестирование** | Vitest + Testing Library | 4.0 / 16.3 | Unit и integration тесты |
| **Линтинг** | ESLint | 9.x | Статический анализ кода |
| **Git Hooks** | Husky + lint-staged | 9.1 / 16.2 | Pre-commit проверки |

---

## 🔐 Безопасность

### Аутентификация
- JWT токены с refresh механизмом
- httpOnly cookies для хранения токенов
- Middleware для защиты роутов
- Автоматический logout при истечении сессии

### Валидация
- Zod-схемы для валидации environment variables
- Серверная валидация всех API-запросов
- Type-safe конфигурация (`src/shared/config/env.ts`)

### CORS и XSS защита
- BFF паттерн устраняет CORS-проблемы
- sameSite: lax для cookies
- secure флаг в production

---

## 📁 Структура проекта

### Ключевые директории

```
project-frontend/
├── .github/workflows/     # CI/CD пайплайн
├── docs/                  # Документация
├── public/                # Статические ассеты
├── src/
│   ├── app/              # Next.js App Router
│   │   ├── api/          # BFF API Routes
│   │   ├── agent/        # Страницы AI-агента
│   │   ├── dashboard/    # Дашборд
│   │   ├── projects/     # Управление проектами
│   │   ├── login/        # Авторизация
│   │   └── register/     # Регистрация
│   ├── entities/         # Бизнес-сущности
│   │   ├── agent-session/
│   │   ├── project/
│   │   └── user/
│   ├── features/         # Функциональность
│   │   ├── auth/
│   │   ├── chat-message/
│   │   ├── create-project/
│   │   └── sber-3d-logo/
│   ├── widgets/          # Композитные блоки
│   │   ├── app-sidebar/
│   │   ├── chat/
│   │   └── landing/
│   └── shared/           # Общий код
│       ├── api/          # HTTP-клиент
│       ├── config/       # Конфигурация
│       ├── hooks/        # React-хуки
│       ├── lib/          # Утилиты
│       ├── store/        # Zustand stores
│       └── ui/           # UI-компоненты
├── Dockerfile            # Контейнеризация
├── vitest.config.ts      # Конфигурация тестов
└── package.json          # Зависимости и скрипты
```

---

## 🚀 CI/CD Pipeline

### GitHub Actions Workflow

```yaml
# Триггеры: push и PR в main/master
jobs:
  quality:     # Lint, Test, Build
  publish:     # Docker build → GHCR (только main)
```

**Этапы качества:**
1. `npm ci` — установка зависимостей
2. `npm run lint` — ESLint проверка
3. `npm run test` — Vitest тесты
4. `npm run build` — Production сборка

**Публикация:**
- Multi-stage Docker сборка (Alpine)
- Публикация в GitHub Container Registry
- Теги: branch, SHA, latest

### Docker

```dockerfile
# Multi-stage build
FROM node:20-alpine AS deps      # Зависимости
FROM node:20-alpine AS builder   # Сборка
FROM node:20-alpine AS runner    # Production
```

**Особенности:**
- Standalone output для минимального размера
- Non-root пользователь (nextjs:nodejs)
- Build-time environment variables

---

## 🧪 Тестирование

### Конфигурация

```typescript
// vitest.config.ts
export default defineConfig({
  plugins: [react()],
  test: {
    environment: "jsdom",
    globals: true,
    include: ["**/*.test.{ts,tsx}"],
  },
});
```

### Запуск тестов

```bash
npm run test          # Однократный запуск
npm run test:watch    # Watch-режим
```

### Pre-commit хуки

```json
"lint-staged": {
  "*.{js,jsx,ts,tsx}": ["eslint --fix", "prettier --write"]
}
```

---

## ⚙️ Скрипты

| Команда | Описание |
|---------|----------|
| `npm run dev` | Development сервер с Turbopack |
| `npm run build` | Production сборка |
| `npm run start` | Запуск production сервера |
| `npm run lint` | ESLint проверка |
| `npm run test` | Запуск тестов |
| `npm run analyze` | Анализ размера бандла |

---

## 🌐 Environment Variables

```env
# Обязательные
NEXT_PUBLIC_API_URL=https://api.example.com
NEXT_PUBLIC_WS_URL=wss://api.example.com/ws

# Опциональные (для Docker)
BACKEND_API_URL=http://backend:8080
```

---

## 📊 Ключевые особенности

### 1. AI-агент интервью
- WebSocket соединение для real-time общения
- Голосовой ввод через Web Speech API
- Streaming ответов агента

### 2. Управление проектами
- CRUD операции
- История сессий
- Генерация требований

### 3. UI/UX
- Адаптивный дизайн (mobile-first)
- Темная/светлая тема
- Анимации Framer Motion
- 3D-логотип (Three.js)

### 4. SEO
- robots.ts и sitemap.ts
- Meta-теги
- Semantic HTML

---

## 📚 Связанная документация

- [ARCHITECTURE.md](../ARCHITECTURE.md) — Визуальные схемы FSD
- [BFF.md](../BFF.md) — Детали BFF-реализации
- [ZUSTAND.md](../ZUSTAND.md) — State management guide
- [FSD-STRUCTURE.md](../FSD-STRUCTURE.md) — Структура по FSD
- [EXAMPLES.md](../EXAMPLES.md) — Примеры использования
- [API-INTEGRATION.md](API-INTEGRATION.md) — Интеграция с API

---

## ✅ Production Readiness

- ✅ Type-safe environment validation (Zod)
- ✅ Pre-commit hooks (Husky + lint-staged)
- ✅ CI/CD pipeline (GitHub Actions → GHCR)
- ✅ Multi-stage Docker build
- ✅ Comprehensive error handling
- ✅ Session expiration handling
- ✅ BFF logging infrastructure

---

*Документ создан: 27 декабря 2024*
