# AGENTS — Справочник проекта для AI-агентов

Этот документ предназначен для **AI-агентов** (LLM-помощников, автоматизированных инструментов разработки), которые работают с данным проектом. Он содержит всю ключевую информацию о проекте: архитектуре, стеке технологий, конвенциях кодирования и правилах работы с кодовой базой.

---

## 📋 Общая информация о проекте

### Название проекта

`project-frontend`

### Описание

Современное Next.js приложение, построенное на архитектуре **Feature-Sliced Design (FSD)** — методологии для создания масштабируемых фронтенд-приложений с чёткой структурой и предсказуемыми зависимостями между модулями.

### Цель проекта

Создание модульного, легко поддерживаемого и масштабируемого фронтенд-приложения с понятной архитектурой и строгими правилами организации кода.

---

## 🛠️ Стек технологий

### Основной стек

- **Next.js 15.5.4** — React-фреймворк с App Router и Server Components
- **React 19.1.0** — библиотека для создания UI
- **TypeScript 5** — статическая типизация
- **Tailwind CSS 4** — utility-first CSS фреймворк
- **Turbopack** — быстрый сборщик (используется в dev и build)

### UI библиотеки

- **Radix UI** — headless компоненты (доступные, композируемые)
- **shadcn/ui** — коллекция переиспользуемых компонентов на базе Radix UI
- **Lucide React** — иконки
- **Remix Icon** — дополнительные иконки
- **class-variance-authority** — управление вариантами компонентов
- **clsx + tailwind-merge** — утилиты для классов (экспортируется через `cn()`)

### State Management

- **Zustand** — легковесная библиотека для управления глобальным состоянием
- **react-hook-form** — управление состоянием форм с валидацией

### Dev tools

- **ESLint 9** — линтинг
- **eslint-config-next** — конфигурация ESLint для Next.js
- **shadcn MCP Server** — Model Context Protocol сервер для работы с shadcn/ui через AI-агентов
- **next-devtools MCP Server** — MCP сервер для работы с Next.js проектом через AI-агентов

### Версионирование и пакетный менеджер

- **npm** (по умолчанию)
- Node.js 20+

---

## 🏗️ Архитектура: Feature-Sliced Design (FSD)

Проект следует методологии **Feature-Sliced Design** — архитектурному подходу для организации кода по слоям и слайсам.

### Основные принципы FSD

1. **Слои (Layers)** — вертикальное разделение по уровню абстракции
2. **Слайсы (Slices)** — горизонтальное разделение по предметной области
3. **Сегменты (Segments)** — группировка файлов по назначению (ui, model, api, lib)
4. **Public API** — каждый модуль экспортирует только то, что должно быть доступно снаружи через `index.ts`

### Иерархия слоёв (от верхнего к нижнему)

```
app         → инициализация, роутинг, провайдеры
  ↓
widgets     → композитные блоки страниц (собраны из features/entities)
  ↓
features    → части функциональности, действия пользователя
  ↓
entities    → бизнес-сущности (модели данных)
  ↓
shared      → переиспользуемый код (UI, utils, hooks, config)
```

### Правила импорта (критически важно!)

✅ **Разрешено:**

- Слой может импортировать **только из нижележащих слоёв**
- `app` → `widgets`, `features`, `entities`, `shared`
- `widgets` → `features`, `entities`, `shared`
- `features` → `entities`, `shared`
- `entities` → `shared`
- `shared` → ничего (самодостаточен)

❌ **Запрещено:**

- Импорт из вышележащих слоёв
- Импорт между модулями одного слоя (widgets → widgets, features → features)
- Импорт из shared в любой другой слой
- Прямой импорт из внутренних файлов модуля (только через Public API)

### Визуальная схема зависимостей

```
┌──────────────────────────────────────┐
│  APP (pages, routing, providers)     │
└──────────────────────────────────────┘
                ↓
┌──────────────────────────────────────┐
│  WIDGETS (композитные блоки)         │
│  - AppSidebar, Chat, SettingsPanel   │
└──────────────────────────────────────┘
                ↓
┌──────────────────────────────────────┐
│  FEATURES (пользовательские действия)│
│  - ChatMessage, TeamSwitcher,        │
│    UserDropdown, SliderControl       │
└──────────────────────────────────────┘
                ↓
┌──────────────────────────────────────┐
│  ENTITIES (бизнес-сущности)          │
│  - (пока пустой слой)                │
└──────────────────────────────────────┘
                ↓
┌──────────────────────────────────────┐
│  SHARED (переиспользуемый код)       │
│  - UI, lib, hooks, config            │
└──────────────────────────────────────┘
```

---

## 📁 Структура проекта

```
project-frontend/
├── public/                      # Статические файлы
├── src/
│   ├── app/                     # App Router (Next.js 15)
│   │   ├── layout.tsx           # Корневой layout
│   │   ├── page.tsx             # Главная страница
│   │   ├── globals.css          # Глобальные стили
│   │   └── dashboard/
│   │       └── page.tsx         # Страница дашборда
│   │
│   ├── widgets/                 # Композитные блоки
│   │   ├── app-sidebar/         # Боковая панель навигации
│   │   │   ├── ui/
│   │   │   │   └── app-sidebar.tsx
│   │   │   └── index.ts         # Public API
│   │   ├── chat/                # Виджет чата
│   │   │   ├── ui/
│   │   │   │   └── chat.tsx
│   │   │   └── index.ts
│   │   └── settings-panel/      # Панель настроек
│   │       ├── ui/
│   │       │   └── settings-panel.tsx
│   │       └── index.ts
│   │
│   ├── features/                # Функциональные компоненты
│   │   ├── chat-message/        # Отображение сообщения
│   │   │   ├── ui/
│   │   │   │   └── chat-message.tsx
│   │   │   └── index.ts
│   │   ├── slider-control/      # Контрол слайдера
│   │   │   ├── ui/
│   │   │   │   └── slider-control.tsx
│   │   │   └── index.ts
│   │   ├── team-switcher/       # Переключатель команд
│   │   │   ├── ui/
│   │   │   │   └── team-switcher.tsx
│   │   │   └── index.ts
│   │   └── user-dropdown/       # Выпадающее меню пользователя
│   │       ├── ui/
│   │       │   └── user-dropdown.tsx
│   │       └── index.ts
│   │
│   ├── entities/                # Бизнес-сущности (готово для расширения)
│   │
│   └── shared/                  # Переиспользуемый код
│       ├── ui/                  # UI компоненты (shadcn/ui)
│       │   ├── button.tsx
│       │   ├── input.tsx
│       │   ├── dialog.tsx
│       │   ├── sidebar.tsx
│       │   ├── ... (всего ~20 компонентов)
│       │   └── index.ts         # Public API (экспорт всех компонентов)
│       ├── lib/                 # Утилиты
│       │   ├── utils.ts         # cn() и другие
│       │   └── index.ts
│       ├── hooks/               # React хуки
│       │   ├── use-mobile.ts
│       │   ├── use-slider-with-input.ts
│       │   ├── use-auth.ts      # (deprecated, используйте store)
│       │   └── index.ts
│       ├── store/               # Zustand stores
│       │   ├── auth-store.ts    # Store для авторизации
│       │   └── index.ts
│       ├── api/                 # API клиент и типы
│       │   ├── client.ts        # HTTP клиент
│       │   ├── types.ts         # TypeScript типы
│       │   └── index.ts
│       └── config/              # Конфигурация (для будущего)
│
├── .vscode/                     # Настройки VS Code
│   └── mcp.json                 # MCP конфигурация
├── components.json              # shadcn/ui конфигурация
├── tsconfig.json                # TypeScript конфигурация
├── next.config.ts               # Next.js конфигурация
├── eslint.config.mjs            # ESLint конфигурация
├── postcss.config.mjs           # PostCSS конфигурация
├── package.json                 # Зависимости и скрипты
├── README.md                    # Основная документация
├── ARCHITECTURE.md              # Визуальные схемы архитектуры
├── FSD-STRUCTURE.md             # Детали FSD структуры
├── EXAMPLES.md                  # Примеры использования
├── CHECKLIST.md                 # Чеклист миграции на FSD
└── AGENTS.md                    # (этот файл)
```

---

## 🎯 Правила работы с кодом

### 1. Именование файлов и компонентов

```typescript
// ✅ Правильно
// Файлы: kebab-case
user-dropdown.tsx
chat-message.tsx
use-mobile.ts

// Компоненты: PascalCase
export function UserDropdown() { ... }
export function ChatMessage() { ... }

// Функции и переменные: camelCase
export function useCurrentUser() { ... }
const isActive = true;

// Типы и интерфейсы: PascalCase
export interface User { ... }
export type MessageStatus = "sent" | "delivered";
```

### 2. Структура модуля (feature/widget/entity)

```
feature-name/
├── ui/                    # UI компоненты
│   └── feature-name.tsx
├── model/                 # Бизнес-логика (опционально)
│   ├── types.ts          # TypeScript типы
│   ├── hooks.ts          # React хуки
│   └── utils.ts          # Утилиты
├── api/                   # API запросы (опционально)
│   └── api.ts
└── index.ts               # Public API (обязательно!)
```

### 3. Public API через index.ts

**Каждый модуль должен экспортировать через `index.ts` только то, что доступно снаружи.**

```typescript
// ✅ Правильно
// src/features/chat-message/index.ts
export { ChatMessage } from "./ui/chat-message";
export type { ChatMessageProps } from "./ui/chat-message";

// ❌ Неправильно
export * from "./ui"; // слишком широкий экспорт
```

### 4. Импорты (порядок и группировка)

```typescript
// 1. React и сторонние библиотеки
import { useState, useEffect } from "react";
import { format } from "date-fns";

// 2. Entities (если есть)
import { User } from "@/entities/user";

// 3. Features (если разрешено слоем)
import { ChatMessage } from "@/features/chat-message";

// 4. Shared (доступен везде)
import { Button, Input, Dialog } from "@/shared/ui";
import { cn } from "@/shared/lib";
import { useIsMobile } from "@/shared/hooks";

// 5. Относительные импорты (внутри модуля)
import { formatMessage } from "./utils";
```

### 5. TypeScript пути (@-импорты)

```typescript
// ✅ Используйте алиасы @/
import { Button } from "@/shared/ui";
import { Chat } from "@/widgets/chat";
import { ChatMessage } from "@/features/chat-message";

// ❌ Избегайте относительных путей между слоями
import { Button } from "../../../shared/ui/button";
```

Настроено в `tsconfig.json`:

```json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

### 6. Компоненты: клиентские vs серверные

**Next.js 15 по умолчанию использует Server Components.**

```typescript
// Серверный компонент (по умолчанию)
// src/widgets/chat/ui/chat.tsx
export function Chat() {
  return <div>Chat</div>;
}

// Клиентский компонент (с интерактивностью)
// src/features/chat-message/ui/chat-message.tsx
("use client"); // ОБЯЗАТЕЛЬНО для useState, useEffect и т.д.

import { useState } from "react";

export function ChatMessage() {
  const [isActive, setIsActive] = useState(false);
  // ...
}
```

### 7. Стилизация (Tailwind CSS)

```typescript
// ✅ Используйте утилиту cn() для условных классов
import { cn } from "@/shared/lib";

<div className={cn(
  "rounded-lg p-4",
  isActive && "bg-blue-500",
  className // props классы всегда последние
)} />

// ✅ Tailwind утилиты напрямую
<Button className="flex items-center gap-2" />
```

### 8. shadcn/ui компоненты

**Все UI компоненты находятся в `src/shared/ui/` и управляются через shadcn CLI.**

```bash
# Добавить новый компонент
npx shadcn@latest add badge

# Компонент автоматически добавится в src/shared/ui/
```

```typescript
// Использование
import { Badge } from "@/shared/ui";

<Badge variant="outline">New</Badge>;
```

### 9. State Management с Zustand

**Zustand — это минималистичная библиотека для управления глобальным состоянием.**

#### Создание store

```typescript
// src/shared/store/my-store.ts
import { create } from "zustand";
import { persist, createJSONStorage } from "zustand/middleware";

interface MyState {
  count: number;
  increment: () => void;
  decrement: () => void;
}

export const useMyStore = create<MyState>()(
  persist(
    (set) => ({
      count: 0,
      increment: () => set((state) => ({ count: state.count + 1 })),
      decrement: () => set((state) => ({ count: state.count - 1 })),
    }),
    {
      name: "my-storage", // ключ в localStorage
      storage: createJSONStorage(() => localStorage),
    }
  )
);
```

#### Использование в компонентах

```typescript
"use client";

import { useMyStore } from "@/shared/store";

export function Counter() {
  // Подписка на весь store
  const { count, increment, decrement } = useMyStore();

  // Или подписка на конкретные поля (оптимизация ре-рендеров)
  const count = useMyStore((state) => state.count);
  const increment = useMyStore((state) => state.increment);

  return (
    <div>
      <p>Count: {count}</p>
      <button onClick={increment}>+</button>
      <button onClick={decrement}>-</button>
    </div>
  );
}
```

#### Auth Store (пример реализации)

```typescript
import { useAuthStore } from "@/shared/store";

// В компоненте
const { user, isAuthenticated, login, logout, isLoading, error } = useAuthStore();

// Авторизация
await login({ email: "test@test.com", password: "123456" });

// Выход
await logout();

// Проверка состояния
if (isAuthenticated) {
  console.log("Пользователь:", user);
}
```

#### Правила работы с Zustand

1. ✅ **Store в `shared/store/`** — глобальные stores только в shared слое
2. ✅ **Одна ответственность** — один store для одной области (auth, cart, ui)
3. ✅ **Типизация** — всегда используйте TypeScript типы
4. ✅ **Middleware persist** — для сохранения состояния между сессиями
5. ✅ **Селекторы** — используйте для оптимизации ре-рендеров
6. ✅ **Async actions** — обрабатывайте loading и error состояния

#### Когда использовать Zustand vs React Context

**Zustand:**
- Глобальное состояние (авторизация, корзина, настройки)
- Нужна персистентность (localStorage)
- Много подписчиков на состояние
- Частые обновления состояния

**React Context:**
- Локальное состояние дерева компонентов
- Провайдеры (theme, i18n)
- Редкие обновления

### 10. shadcn MCP Server

**Проект настроен для работы с Model Context Protocol (MCP) — открытым протоколом для интеграции AI-агентов с инструментами разработки.**

МCP сервер shadcn предоставляет AI-агентам прямой доступ к:

- Списку доступных компонентов в registries
- Просмотру деталей компонентов (код, зависимости, типы)
- Поиску компонентов по запросу
- Примерам использования компонентов
- Командам для добавления компонентов в проект

**Конфигурация MCP:**

```json
// .vscode/mcp.json
{
  "servers": {
    "shadcn": {
      "command": "npx",
      "args": ["shadcn@latest", "mcp"]
    },
    "next-devtools": {
      "command": "npx",
      "args": ["-y", "next-devtools-mcp@latest"]
    }
  }
}
```

**Доступные MCP инструменты для AI-агентов:**

- `mcp_shadcn_get_project_registries` — получить настроенные registries из components.json
- `mcp_shadcn_list_items_in_registries` — список всех компонентов в registries
- `mcp_shadcn_search_items_in_registries` — поиск компонентов (fuzzy search)
- `mcp_shadcn_view_items_in_registries` — детальная информация о компонентах
- `mcp_shadcn_get_item_examples_from_registries` — примеры использования компонентов
- `mcp_shadcn_get_add_command_for_items` — получить команду для добавления компонентов

**Для AI-агентов:**

При работе с UI компонентами используйте MCP инструменты для:

1. Поиска нужного компонента: `search_items_in_registries`
2. Просмотра деталей и API: `view_items_in_registries`
3. Изучения примеров: `get_item_examples_from_registries`
4. Получения команды установки: `get_add_command_for_items`

Пример workflow:

```typescript
// 1. Найти компонент
// mcp_shadcn_search_items_in_registries({ query: "badge", registries: ["@shadcn"] })

// 2. Посмотреть детали
// mcp_shadcn_view_items_in_registries({ items: ["@shadcn/badge"] })

// 3. Получить примеры
// mcp_shadcn_get_item_examples_from_registries({ query: "badge-demo", registries: ["@shadcn"] })

// 4. Добавить в проект (если нужно)
// mcp_shadcn_get_add_command_for_items({ items: ["@shadcn/badge"] })
// Вернёт: "npx shadcn@latest add badge"
```

**Важно:** Все UI компоненты уже установлены в `src/shared/ui/`. Перед добавлением нового компонента проверьте, не существует ли он уже в проекте.

### 11. Next.js Devtools MCP Server

**Проект настроен для работы с next-devtools MCP сервером — инструментом для взаимодействия с Next.js проектом через AI-агентов.**

МCP сервер next-devtools предоставляет AI-агентам возможности:

- Анализ структуры Next.js проекта
- Работа с роутингом и страницами
- Инспекция компонентов и их зависимостей
- Отладка и диагностика проблем
- Получение информации о конфигурации проекта

**Для AI-агентов:**

Используйте next-devtools MCP сервер для:

1. Анализа структуры проекта и роутинга
2. Диагностики проблем с производительностью
3. Инспекции Server/Client Components
4. Работы с App Router и страницами
5. Получения информации о build и runtime

---

## 🚀 Команды для работы с проектом

```bash
# Установка зависимостей
npm install

# Запуск dev сервера (Turbopack)
npm run dev
# Откроется http://localhost:3000

# Production сборка (Turbopack)
npm run build

# Запуск production сервера
npm run start

# Линтинг
npm run lint
```

---

## 📝 Инструкции для AI-агентов

### При добавлении нового функционала

1. **Определите тип компонента:**

   - Большой композитный блок → `widgets/`
   - Действие пользователя → `features/`
   - Бизнес-сущность → `entities/`
   - Переиспользуемый UI → `shared/ui/`

2. **Создайте правильную структуру:**

   ```bash
   mkdir -p src/features/my-feature/ui
   touch src/features/my-feature/ui/my-feature.tsx
   touch src/features/my-feature/index.ts
   ```

3. **Следуйте правилам импорта:**

   - Проверьте, что импортируете только из нижележащих слоёв
   - Используйте Public API (через `index.ts`)

4. **Обновите Public API:**
   ```typescript
   // src/features/my-feature/index.ts
   export { MyFeature } from "./ui/my-feature";
   export type { MyFeatureProps } from "./ui/my-feature";
   ```

### При модификации существующего кода

1. **Проверьте текущую архитектуру:**

   - Посмотрите, в каком слое находится файл
   - Убедитесь, что ваши изменения не нарушают правила FSD

2. **Обновляйте импорты:**

   - Если меняете расположение файла, обновите все импорты
   - Используйте @-алиасы для импортов между слоями

3. **Не нарушайте Public API:**
   - Если меняете экспорты в `index.ts`, проверьте зависимости

### При рефакторинге

1. **Миграция компонента в другой слой:**

   - Переместите файлы в новую структуру
   - Создайте `index.ts` с Public API
   - Обновите все импорты в проекте
   - Удалите старые файлы

2. **Разделение большого компонента:**
   - Вынесите логику в `model/`
   - Разделите UI на части в `ui/`
   - Экспортируйте только главный компонент через `index.ts`

---

## ⚠️ Критические правила (нарушение ломает архитектуру)

1. **НЕ импортируйте из вышележащих слоёв**

   ```typescript
   // ❌ НЕПРАВИЛЬНО (features → widgets)
   import { Chat } from "@/widgets/chat"; // в features/

   // ✅ ПРАВИЛЬНО (features → shared)
   import { Button } from "@/shared/ui";
   ```

2. **НЕ импортируйте между модулями одного слоя**

   ```typescript
   // ❌ НЕПРАВИЛЬНО (features → features)
   import { ChatMessage } from "@/features/chat-message"; // в другой feature

   // ✅ ПРАВИЛЬНО: вынесите общее в entities или shared
   import { Message } from "@/entities/message";
   ```

3. **НЕ импортируйте напрямую из внутренних файлов**

   ```typescript
   // ❌ НЕПРАВИЛЬНО
   import { ChatMessage } from "@/features/chat-message/ui/chat-message";

   // ✅ ПРАВИЛЬНО (через Public API)
   import { ChatMessage } from "@/features/chat-message";
   ```

4. **Всегда используйте "use client" для клиентских компонентов**

   ```typescript
   // ❌ НЕПРАВИЛЬНО (будет ошибка)
   import { useState } from "react";
   export function MyComponent() {
     const [state, setState] = useState(false);
     // ...
   }

   // ✅ ПРАВИЛЬНО
   ("use client");
   import { useState } from "react";
   export function MyComponent() {
     const [state, setState] = useState(false);
     // ...
   }
   ```

---

## 📚 Дополнительная документация

- **README.md** — быстрый старт и основная информация
- **ARCHITECTURE.md** — визуальные схемы архитектуры
- **FSD-STRUCTURE.md** — детальное описание FSD структуры
- **EXAMPLES.md** — примеры создания features, widgets, entities
- **CHECKLIST.md** — чеклист миграции на FSD (для понимания процесса)

---

## 🔗 Полезные ссылки

- [Feature-Sliced Design](https://feature-sliced.design/) — официальная документация FSD
- [Next.js 15 Documentation](https://nextjs.org/docs) — документация Next.js
- [shadcn/ui](https://ui.shadcn.com/) — UI компоненты
- [Tailwind CSS](https://tailwindcss.com/) — документация Tailwind
- [Radix UI](https://www.radix-ui.com/) — headless компоненты

---

## 🤖 Для LLM-агентов: Контрольный чеклист

Перед выполнением задачи проверьте:

- [ ] Я понимаю, в каком слое находится компонент (app/widgets/features/entities/shared)
- [ ] Я знаю правила импорта для этого слоя
- [ ] Я использую @-алиасы для импортов (@/shared/ui, @/features/...)
- [ ] Я создаю/обновляю Public API через index.ts
- [ ] Я использую "use client" для клиентских компонентов
- [ ] Я следую конвенциям именования (kebab-case для файлов, PascalCase для компонентов)
- [ ] Я не нарушаю зависимости между слоями
- [ ] Я использую cn() для условных классов
- [ ] Я использую компоненты из @/shared/ui (shadcn/ui)
- [ ] Я добавил TypeScript типы для props и данных
- [ ] Я использую Zustand для глобального состояния (не Context API)
- [ ] Я создаю stores в shared/store/ с правильной типизацией
- [ ] Я проверил доступные компоненты через MCP инструменты перед добавлением новых
- [ ] Я изучил примеры использования компонентов через get_item_examples_from_registries

---

**Последнее обновление:** 29 октября 2025  
**Версия документа:** 1.0  
**Статус проекта:** Активная разработка, архитектура стабильна
